---
import { Code } from "astro:components";
import { codeBlock } from "./CodeBlock.css";

interface Props {
	code: string;
}

const { code } = Astro.props;
---
<Code class={codeBlock} code={code} lang="ts" theme="one-dark-pro" />